//main class of Snake       
class Main {

    /** Initializes a Snake game and starts running it. */
    function void main() {
        var Snake game;
        do Snake.newInstance();
        let game = Snake.getInstance();
        do game.run();
      do game.dispose();
        return;
    }
}
